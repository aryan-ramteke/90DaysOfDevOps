kind: Pod
apiVersion: v1

metadata:
  name: nginx-pod
  namespace: nginx-ns
spec:
  containers:
    - name: nginx-container
      image: nginx:latest
      ports:
        - containerPort: 80
          name: http
          protocol: TCP
